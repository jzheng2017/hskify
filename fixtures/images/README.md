# Deterministic contract image

`gate0-source.png` is a project-authored geometric input used only by focused
pipeline contract tests. It is not evidence of manga/webtoon visual quality
and must not be used for release quality claims.

The former synthetic panel and long-reader artwork has been removed. Browser
and end-to-end quality regressions use the ignored local real-reader corpus
described in `fixtures/real-reader-corpus/README.md`.
